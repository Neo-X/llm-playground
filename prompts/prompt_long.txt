Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that GPU compute and memory bandwidth are both kept busy without starving any single request of progress. Continuous batching techniques allow new requests to join an in-flight batch as soon as a slot frees up, rather than waiting for the entire batch to finish, which substantially improves throughput under variable load. Quantization reduces the number of bits used to represent each weight, trading a small amount of accuracy for large reductions in memory footprint and memory bandwidth, which matters most during the decode phase since every generated token requires reading the full set of weights and the growing key-value cache from memory. Serving large language models efficiently requires careful attention to batching, quantization, and memory management across the entire inference stack. When multiple requests arrive concurrently, a scheduler must decide how to interleave their prefill and decode phases so that...

Given all of that context, explain in 5 short bullet points how batching and quantization choices interact to affect end-to-end inference throughput.