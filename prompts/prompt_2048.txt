Transformer inference latency is shaped by two distinct phases: prefill, where the entire prompt is processed in parallel to populate the key-value cache, and decode, where tokens are generated one at a time autoregressively. Prefill throughput is typically compute-bound because the GPU can batch all prompt tokens into large matrix multiplications, while decode throughput is usually memory-bandwidth-bound because each step re-reads the entire KV cache and model weights to produce just one new token. Transformer inference latency is shaped by two distinct phases: prefill, where the entire prompt is processed in parallel to populate the key-value cache, and decode, where tokens are generated one at a time autoregressively. Prefill throughput is typically compute-bound because the GPU can batch all prompt tokens into large matrix multiplications, while decode throughput is usually memory-bandwidth-bound because each step re-reads the entire KV cache and model weights to produce just one new token. Transformer inference latency is shaped by two distinct phases: prefill, where the entire prompt is processed in parallel to populate the key-value cache, and decode, where tokens are generated one at a time autoregressively. Prefill throughput is typically compute-bound because the GPU can batch all prompt tokens into large matrix multiplications, while decode throughput is usually memory-bandwidth-bound because each step re-reads the entire KV cache and model weights to produce just one new token. Transformer inference latency is shaped by two distinct phases: prefill, where the entire prompt is processed in parallel to populate the key-value cache, and decode, where tokens are generated one at a time autoregressively. Prefill throughput is typically compute-bound because the GPU can batch all prompt tokens into large matrix multiplications, while decode throughput is...

Given all of that context, explain in 5 short bullet points how transformer inference latency is measured, referencing both prefill and decode phases.
